@model IEnumerable<WebApplication2.Models.OrderDetail>
    

@{
    ViewBag.Title = "title";
    Layout = "_CoffeeLayout";
}


<h1 style="margin-bottom: 10px"> Order Detail Home </h1>

<div>
    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>SaleId</th>
            <th>CoffeeID</th>
            <th>Quantity</th>
        </tr>
        </thead>
        <tbody>
        @{
            foreach (var OrderDetail in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(orderDetailList => OrderDetail.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(orderDetailList => OrderDetail.SaleId)
                    </td>
                    <td>
                        @Html.DisplayFor(orderDetailList => OrderDetail.CoffeeId)
                    </td>
                    <td>
                        @Html.DisplayFor(orderDetailList => OrderDetail.Quantity)
                    </td>
                    @* <td> *@
                    @*     @Html.DisplayFor(saleList => sales.OrderDetails) *@
                    @* </td> *@
                    <td>
                        <a asp-action="Edit" asp-route-id="@OrderDetail.Id">Edit</a>
                        @* <a asp-action="Details" asp-route-id="@customer.Id">Details</a> *@
                        <a asp-action="Delete" asp-route-id="@OrderDetail.Id">Delete</a>
                    </td>
                </tr>
            }
        }
        </tbody>
    </table>
</div>
